server:
  port: 8090

spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      server:
        webflux:
          routes:
            # Route all document API requests to document-service.
            - id: document-service
              uri: http://localhost:8080
              predicates:
                - Path=/api/documents,/api/documents/**
              filters:
                - StripPrefix=1

management:
  endpoints:
    web:
      exposure:
        include: health
